# VibeStack Termius Import Configuration
# Copy this file to .env and customize for your setup
# The .env file is automatically ignored by git for security

# SSH Connection Settings
SSH_USERNAME=ubuntu
SSH_PORT=22
SSH_KEY_PATH=~/.ssh/id_rsa

# Paths and Directories
OUTPUT_DIR=./termius-import
TERRAFORM_DIR=./terraform/vibestack

# Optional: Custom tags for Termius
# CUSTOM_TAGS=mycompany,production,vibestack

# Optional: Alternative SSH key for OCI instances
# SSH_KEY_PATH=~/.ssh/oci-vibestack-key

# Optional: If using a different OS image
# SSH_USERNAME=opc  # for Oracle Linux
# SSH_USERNAME=ec2-user  # for some other distributions